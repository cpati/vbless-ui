apiVersion: v1 # for versions before 1.9.0 use apps/v1beta2
kind: Service
metadata:
  labels:
    app: vbless-ui
  name: vbless-ui
spec:
  selector:
    app: vbless-ui
    tier: frontend
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 80
  type: LoadBalancer  
